The program requires some python packages.
Run the command to install all the required function
    sudo pip3 install -r requirements.txt

The directory contains 3 distorted files and a training file, 
training file is already loaded in the program

    distorted_a.csv
        Distorted A values
    distorted_b.csv
        Distorted B values
    distorted_c.csv
        Distorted C values